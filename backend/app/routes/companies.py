"""[Deprecated in V2]

The global `/api/companies` routes have been removed; use the team-scoped
equivalents under `/api/teams/{team_slug}/companies/...` implemented in
`app.routes.teams`.

Kept as a stub so legacy imports fail loudly rather than silently.
"""
