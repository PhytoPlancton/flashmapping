"""[Deprecated in V2]

The global `/api/contacts` routes have been removed; use the team-scoped
equivalents under `/api/teams/{team_slug}/contacts/...` and
`/api/teams/{team_slug}/companies/{company_slug}/contacts` implemented in
`app.routes.teams`.
"""
